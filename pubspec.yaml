name: address
description: Addresses internationalization library.
version: 0.1.0

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  meta: ^1.3.0

dev_dependencies:
  lint: ^1.5.1
  test: ^1.16.4

